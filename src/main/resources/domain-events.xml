<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration" xmlns:int-jms="http://www.springframework.org/schema/integration/jms"
	xmlns:amq="http://activemq.apache.org/schema/core"
	xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/jms http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd
		http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<int:annotation-config />

	<int:publish-subscribe-channel id="eventBus" />

	<int-jms:outbound-channel-adapter channel="eventBus" connection-factory="jmsConnectionFactory"
		destination="sampleQueue" />

	<int:channel id="logChannel" />
	<int:wire-tap pattern="eventBus" channel="logChannel" />
	<int:logging-channel-adapter id="logChannel" level="INFO" />

	<amq:queue id="sampleQueue" physicalName="sampleQueue" />
	<amq:queue id="sampleQueueDlq" physicalName="DLQ.sampleQueue" />

	<bean id="messagingTemplate" class="org.springframework.integration.core.MessagingTemplate">
		<constructor-arg ref="eventBus" />
	</bean>

</beans>
